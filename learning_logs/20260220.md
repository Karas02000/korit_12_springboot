# JPA를 이용한 DB 생성 및 접근
## JPA
1. 정의
JPA(Jakarta Persistence API)의 축약어로 엔티티 클래스를 통해 DB를 정의하는 방법.
즉, HeidiSQL을 이용하여 DB를 생성하고 TABLE 생성, COLUMN 정의, SQL문을 활용하여 row들을 추가, 수정, 삭제, 조회를 했던 것과 달리 Spring Boot 프로젝트 내에서 해결을 한다는 것이다.

